-content_for( :js_include ) do
  javascript:
    function boot(){
      Fabnavi.play();
    }

javascript:
  PICTURES_DATA = #{{@pictures.to_json}}
  PROJECT_DATA = #{{@project.to_json}}

== render 'player'
